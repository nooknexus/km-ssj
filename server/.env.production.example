# Database (Server Production DB)
DB_HOST=localhost
DB_USER=km_user
DB_PASSWORD=secure_password
DB_NAME=km_health_db

# Security
JWT_SECRET=super_secret_key_change_this_in_production

# Server
PORT=5001
CORS_ORIGIN=https://km.plkhealth.go.th

# SSO Health ID (Must register https://km.plkhealth.go.th/api/auth/healthid as Redirect URI)
HEALTH_CLIENT_ID=production_client_id
HEALTH_CLIENT_SECRET=production_client_secret
HEALTH_REDIRECT_URI=https://km.plkhealth.go.th/api/auth/healthid

# Frontend URL (For redirects after login)
FRONTEND_URL=https://km.plkhealth.go.th
